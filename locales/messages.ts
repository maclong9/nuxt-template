import de from './de'
import en from './en'

/**
 * A default export object containing translations for supported languages.
 * Currently includes:
 * - 'en': English
 * - 'de': German
 */
export default {
  en,
  de,
}
